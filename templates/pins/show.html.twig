{% extends 'base.html.twig' %}

{% block main %}

	<a href="{{path('app_home')}}">Retour à l'accueil</a>

	<h1>{{pin.title}}</h1>

	<p>{{pin.description}}</p>

	<a href="{{path('app_pins_edit', {'id': pin.id})}}">Modifier le Pin</a>

	<a href="#" onclick="event.preventDefault(); confirm('confirmez-vous la suppression du Pin?') && document.getElementById('js-pin-delete-form').submit();">Supprimer le Pin</a>

 	<form id="js-pin-delete-form" action="{{path('app_pins_delete', {'id': pin.id})}}" method="POST" style="display: none;">
 		<input type="hidden" name="csrf_token" value="{{ csrf_token('pin_deletion_' ~ pin.id) }}">
 		<input type="hidden" name="_method" value="DELETE">
 	</form>

{% endblock %}